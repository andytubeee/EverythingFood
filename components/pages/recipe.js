import React from 'react';
import {View, Text, StyleSheet} from 'react-native';
import {Header} from '../Header';

const receipe = () => {
  return (
    <View>
      <Header text="Recipe" color={'#12c471'} />
      <Text>Recipe Page</Text>
    </View>
  );
};

export default receipe;
